/**
 * Default quotas when subscription is not configured.
 * Used by student-onboard and automation-flow when subscription records are removed.
 */
export const DEFAULT_BASE_USER_QUOTA = 100
export const DEFAULT_NOTIFICATION_QUOTA = 1000
